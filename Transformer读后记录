Transformer 
一 构成
1.1  Transformer块【MHA(多头注意力) -> Add & Norm(残差和后归一化层) -> Positionwise FFN(逐位置（对每个token）前馈神经网络) -> Add & Norm(残差和后归一化层)】

关于Transformer块的思考：
1.1.1  Position-wise FFN:接收前面MHA里从原始特征间的相关性（全文融合Context）,对每一个token进行各自分析（升维降维）。
        -> 构成：两个全连接层中间加个非线性激活函数。
        -> 意义：引入非线性；解耦与特征重组（MHA 负责融合不同词语，FFN 负责融合/提炼同一个词语内部的不同特征维度）；记忆库假说（参数占比全部约2/3）
        -> 现在的改进:经典的 FFN 已经被升级为 SwiGLU 等门控前馈网络结构（原因简单来说就是模型动态决定不同特征对结果的权重）
        -> 该层会对每一个token处理，这里可以精简吗？举个例子：当大模型看到“法国的首都”经过 MHA处理后，它的特征向量进入FFN 
           这句话应该有3个token，每个都会在FFN处理，但是这里的关键的首都这个token，经过mha它已经包含法国的信息，为什么不能只对这个token做FFN，其他的其实没必要？
           回答：在多层（Multi-Layer）网络结构下，前面的 token 必须经过 FFN 的加工，才能在更深层为后面的 token 提供有价值的信息（不能单独“升级”，要一起“升级”，真的需要这么多层吗？）。
                此外，还有训练机制和底层硬件（并行）的原因。为什么必须所有 token 都做 FFN？
                因为底层要为高层做准备。如果不全做，前面的词就无法在深层网络中进化为高级语义概念，导致后面的核心词在做 MHA 时“无好东西可看”。
                全做 FFN 是一种为了硬件并行计算极其高效而做出的暴力美学妥协。而打破这种妥协，让计算按需分配，正是当前大模型优化的核心方向之一！
                不过确实可以该进，例如现在的llm标配之一：MoE（混合专家模型）与动态路由：不是每个 token 都过同样庞大的 FFN。
                简单的 token 被分配给极小的 FFN 算一算就好，只有像“首都”这种高信息密度的词，才会被激活分配给多个庞大的专家（FFN）去深加工。

1.1.2 MHA(多头注意力)：负责跨Token的信息融合（找 Context），不同token之间的匹配度，相关性。
      -> 核心：self-attention(QKV机制)。Q (Query / 查询)：代表“我需要什么信息？”;K (Key / 键)：代表“我包含了什么特征？”;V (Value / 值)：代表“如果别人需要我，我能提供什么实质性的内容？”
      -> 现在的改进：MQA（Multi-Query Attention）与 GQA（Grouped-Query Attention）：
                    在推理时，保存所有历史 Token 的 K 和 V 极其耗费显存（这就是 KV Cache 刺客）。为了省内存，现代模型不再让每个 Head 都拥有自己独立的 K 和 V。
                    MQA：所有 Q 共享同一组 K 和 V。（极其省内存，但性能略有下降）
                    GQA：对 Head 进行分组，比如 8 个 Q 头共享 2 个 K/V 头。（Llama 等当红模型的标配，内存和性能的完美平衡）
1.1.3 Add&Norm：残差和层归一化‘
      -> 现在的改进：Attention—Residual(残差参数不在默认是1，引入softmax思想根据权重决定不同层的重要性)和RMSNorm（去均值，只计算均方根（有研究表明取均值对模型影响不大））。

问题1：为什么大模型多用 Decoder-only 而不用 Encoder-Decoder？
回答：从架构上来说Decoder-only更简洁，没有跨注意力机制减少了计算；
      从目标上Decoder-only只是自回归问题范式更简单，而Encoder-Decoder的底层思想是先理解输入再生成输出

拓展：
结构化信息:信息精确，便于理解（机器或人）
普通句子（非结构化）:小明在图书馆借了一本书。
结构化语义表示:
动作：借
人物：小明
地点：图书馆
对象：书
